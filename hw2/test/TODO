1. Use timeit first
2. Using cProfile and visualizing pstats
- python -m cProfile -o test.pstats test.py
- python -m pstats test.pstats
- gprof2dot -f pstats test.pstats | dot -Tpddf -o output.pdf
- cumtime = spend in function, total_time = time in callee
3. Remove string concantenation
4. Using line_profiler
5. Using massif

Testing
1. Create class with a set_up method
2. Using text fixture
3. Plugins: Tox, 
4. with pytest.raises(Exception): f()
5. setup_module(), teardown_module()
6. import mock
